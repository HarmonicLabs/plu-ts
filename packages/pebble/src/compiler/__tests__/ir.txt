function §SumInputLove_1(
    const §ctx_2: ScriptContext
  ): void {

    return (case /*letted '§scriptcontext_9_purpose_e'*/(
      fromData(/*letted '§scriptcontext_9_fields_b'*/(
      /*letted '§scriptcontext_9_fields_b'*/(
      <native sndPair>(
        /*letted '§scriptcontext_9_unconstrPair_a'*/(
        <native unConstrData>(
          /*letted '§scriptcontext_9'*/(
          §ctx_2
          )
        )
        )
      )
      )
      )[ 2 ]) as ScriptInfo
      )
      is const Spend{
        ref: const §spendingRef_6: TxOutRef,
        optionalDatum: const §optionalDatum_7: Optional<data>
      } => (case function §loop_i(
          let tot: int,
          let i: int
        ): §__StmtSideEffects_h {

          return ((i <= 10) ?
            §loop_i(
              /*letted '§tot_j'*/(
              (/*letted '§tot_j'*/(
              (§tot_k + 1)
              ) + 1)
              ),
              i
            ) :
            Reassigns{ tot: tot })
        }(
          /*letted 'tot'*/(
          0
          ),
          1
        )
        is let Reassigns{
          tot: const §tot_k: int
        } => assert (§tot_k === 55)
        then void
      )
      else (fail)
    )
  }