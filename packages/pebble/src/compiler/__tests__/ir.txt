function §HelloWorld_52(
  const §ctx_0b: ScriptContext
): void {

  const {
    tx: const §tx_5c: Tx,
    purpose: const §purpose_f8: ScriptInfo,
    redeemer: const §redeemer_17: data
  } = §ctx_0b;

  match (
    §purpose_f8
  ) {
    when const Spend{
      ref: const §spendingRef_37d7: TxOutRef,
      optionalDatum: const §optionalDatum_2d18: Optional<data>
    }: {

      const {
        owner: const owner: bytes
      } = ((case §optionalDatum_2d18
        is const Some{
          value: const value: data
        } => value
        is const None{} => (fail)
      ) as HelloWorldDatum);
      assert §tx_5c.requiredSigners.includes(
        owner
      );
      return void
    };
    else fail;
  };

  fail
}