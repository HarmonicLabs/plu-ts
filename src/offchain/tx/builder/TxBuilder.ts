import ProtocolParamters from "../../ledger/protocol/ProtocolParameters";
import BlockchainProvider from "./BlockchainProvider";

export class TxBuilder
{
    constructor( provider: BlockchainProvider, protocolParamters: ProtocolParamters )
    {
        
    }
}

export default TxBuilder;