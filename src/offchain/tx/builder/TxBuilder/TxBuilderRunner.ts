import { IProvider } from "./IProvider";
import { TxBuilder } from "./TxBuilder";

export class TxBuilderRunner
{
    constructor(
        txBuilder: TxBuilder,
        provider: IProvider
    )
    {
       
    }
}